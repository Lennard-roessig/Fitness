import 'package:flutter/material.dart';

class TimerScreen extends StatelessWidget {
  static const route = "timer/create/";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('Timer View'),
    );
  }
}
